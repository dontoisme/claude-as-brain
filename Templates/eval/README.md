# Eval Fixture

`questions.md` holds known-answer questions for `/self-check`.

Part A works against the shipped example seed. Part B is where you write
questions against your own notes after the seed is cleared — that's the part
that actually tells you anything.

These are fixtures, not notes. They're excluded from `/link-check`,
`/update-mocs`, and `/ask` sweeps.
